dependencies:
  pre:
    - ./circleci.sh install-deps
    - ./circleci.sh install-make
    - ./circleci.sh setup-repos
  cache_directories:
    - "~/dlang"

test:
  override:
    - ./circleci.sh run-make
